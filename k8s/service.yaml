apiVersion: v1
kind: Service
metadata:
  name: {{PROJECT_NAME}}
  namespace: {{NAMESPACE}}
  labels:
    app: {{PROJECT_NAME}}
spec:
  type: ClusterIP
  selector:
    app: {{PROJECT_NAME}}
  ports:
    - port: 80
      targetPort: 8000
